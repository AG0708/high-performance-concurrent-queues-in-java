# Resume bullets

- Built concurrent FIFO queues in Java using atomic slot claims, release/acquire memory ordering, and cache-separated producer and consumer cursors.
- Verified FIFO behavior with randomized linearizability checks, high-contention stress tests, and JCStress; measured performance against JDK queues with JMH.

Add a benchmark number only from a committed release evidence directory. Keep the thread count, batch size, machine, and baseline attached to the claim.
